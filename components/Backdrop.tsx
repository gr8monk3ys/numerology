/**
 * Ambient backdrop: a fading dot grid and a soft gilt glow at the top of the
 * page, drawn once behind all content.
 */
export function Backdrop() {
  return <div className="backdrop" aria-hidden="true" />;
}
